@extends('layouts.app')
@section('content')
<section class="max-w-3xl mx-auto px-4 py-12"><h1 class="text-3xl font-semibold">{{ __('messages.nav_contact') }}</h1><form method="post" action="{{ route('contact.store') }}" class="mt-6 space-y-4 bg-white rounded-2xl p-6">@csrf<select name="inquiry_type" class="w-full border rounded-xl px-3 py-2"><option value="general">{{ __('messages.general') }}</option><option value="collaboration">{{ __('messages.collaboration') }}</option><option value="vendor">{{ __('messages.vendor_request') }}</option></select><input name="name" class="w-full border rounded-xl px-3 py-2" placeholder="{{ __('messages.name') }}"><input name="email" class="w-full border rounded-xl px-3 py-2" placeholder="Email"><input name="subject" class="w-full border rounded-xl px-3 py-2" placeholder="{{ __('messages.subject') }}"><textarea name="message" class="w-full border rounded-xl px-3 py-2" rows="6" placeholder="{{ __('messages.message') }}"></textarea><button class="bg-terracotta text-white rounded-xl px-4 py-2">{{ __('messages.send') }}</button></form></section>
@endsection
