@extends('layouts.app')
@section('content')
<section class="max-w-7xl mx-auto px-4 py-12"><h1 class="text-3xl font-semibold mb-6">{{ __('messages.nav_marketplace') }}</h1><form class="mb-6"><input name="q" value="{{ request('q') }}" class="rounded-xl border px-3 py-2" placeholder="{{ __('messages.search_products') }}"></form><div class="grid md:grid-cols-4 gap-4">@foreach($products as $product)<a href="{{ route('marketplace.show',$product->slug) }}" class="bg-white p-4 rounded-2xl border border-sand/80"><h3 class="font-semibold">{{ $product->name }}</h3><p class="text-terracotta mt-2">{{ number_format($product->price,2) }} TND</p><p class="text-xs text-charcoal/70 mt-1">{{ __('messages.stock') }}: {{ $product->stock }}</p></a>@endforeach</div><div class="mt-6">{{ $products->links() }}</div></section>
@endsection
