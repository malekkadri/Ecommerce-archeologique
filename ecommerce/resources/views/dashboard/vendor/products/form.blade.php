<section class="max-w-3xl mx-auto px-4 py-12"><form method="post" action="{{ $action }}" class="bg-white p-6 rounded-2xl space-y-3">@csrf @if($method==='PUT') @method('PUT') @endif<input name="name" class="w-full border rounded px-3 py-2" placeholder="Name" value="{{ old('name',optional($product)->name) }}"><input name="slug" class="w-full border rounded px-3 py-2" placeholder="slug" value="{{ old('slug',optional($product)->slug) }}"><textarea name="description" class="w-full border rounded px-3 py-2">{{ old('description',optional($product)->description) }}</textarea><input name="price" class="w-full border rounded px-3 py-2" value="{{ old('price',optional($product)->price) }}"><input name="stock" class="w-full border rounded px-3 py-2" value="{{ old('stock',optional($product)->stock) }}"><button class="bg-terracotta text-white rounded px-4 py-2">{{ __('messages.save') }}</button></form></section>
